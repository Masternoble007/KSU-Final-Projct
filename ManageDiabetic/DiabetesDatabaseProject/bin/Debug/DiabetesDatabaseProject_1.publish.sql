/*
Deployment script for Diabetesdb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Diabetesdb"
:setvar DefaultFilePrefix "Diabetesdb"
:setvar DefaultDataPath "C:\Users\sja48\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\LocalDiabetesServer\"
:setvar DefaultLogPath "C:\Users\sja48\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\LocalDiabetesServer\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Dropping Foreign Key unnamed constraint on [dbo].[AddedInsulin]...';


GO
ALTER TABLE [dbo].[AddedInsulin] DROP CONSTRAINT [FK__AddedInsu__Insul__2C3393D0];


GO
PRINT N'Dropping Foreign Key unnamed constraint on [dbo].[Measurement]...';


GO
ALTER TABLE [dbo].[Measurement] DROP CONSTRAINT [FK__Measureme__Added__2D27B809];


GO
PRINT N'Dropping Foreign Key unnamed constraint on [dbo].[MeasurementFood]...';


GO
ALTER TABLE [dbo].[MeasurementFood] DROP CONSTRAINT [FK__Measureme__FoodI__2F10007B];


GO
PRINT N'Dropping Foreign Key unnamed constraint on [dbo].[MeasurementFood]...';


GO
ALTER TABLE [dbo].[MeasurementFood] DROP CONSTRAINT [FK__Measureme__Measu__2E1BDC42];


GO
PRINT N'Starting rebuilding table [dbo].[AddedInsulin]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_AddedInsulin] (
    [AddedInsulinId] INT IDENTITY (1, 1) NOT NULL,
    [InsulinId]      INT NOT NULL,
    [InsulinAmount]  INT NOT NULL,
    PRIMARY KEY CLUSTERED ([AddedInsulinId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[AddedInsulin])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_AddedInsulin] ON;
        INSERT INTO [dbo].[tmp_ms_xx_AddedInsulin] ([AddedInsulinId], [InsulinId], [InsulinAmount])
        SELECT   [AddedInsulinId],
                 [InsulinId],
                 [InsulinAmount]
        FROM     [dbo].[AddedInsulin]
        ORDER BY [AddedInsulinId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_AddedInsulin] OFF;
    END

DROP TABLE [dbo].[AddedInsulin];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_AddedInsulin]', N'AddedInsulin';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Food]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Food] (
    [FoodId] INT  IDENTITY (1, 1) NOT NULL,
    [Name]   TEXT NOT NULL,
    [Brand]  TEXT NULL,
    [Carbs]  REAL NULL,
    PRIMARY KEY CLUSTERED ([FoodId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Food])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Food] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Food] ([FoodId], [Name], [Brand], [Carbs])
        SELECT   [FoodId],
                 [Name],
                 [Brand],
                 [Carbs]
        FROM     [dbo].[Food]
        ORDER BY [FoodId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Food] OFF;
    END

DROP TABLE [dbo].[Food];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Food]', N'Food';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Insulin]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Insulin] (
    [InsulinId] INT  IDENTITY (1, 1) NOT NULL,
    [Name]      TEXT NOT NULL,
    [Brand]     TEXT NOT NULL,
    PRIMARY KEY CLUSTERED ([InsulinId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Insulin])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Insulin] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Insulin] ([InsulinId], [Name], [Brand])
        SELECT   [InsulinId],
                 [Name],
                 [Brand]
        FROM     [dbo].[Insulin]
        ORDER BY [InsulinId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Insulin] OFF;
    END

DROP TABLE [dbo].[Insulin];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Insulin]', N'Insulin';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Measurement]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Measurement] (
    [MeasurementId]  INT  IDENTITY (1, 1) NOT NULL,
    [Date]           TEXT NOT NULL,
    [Time]           TEXT NOT NULL,
    [MeasuredValue]  REAL NULL,
    [AddedInsulinId] INT  NULL,
    PRIMARY KEY CLUSTERED ([MeasurementId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Measurement])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Measurement] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Measurement] ([MeasurementId], [Date], [Time], [MeasuredValue], [AddedInsulinId])
        SELECT   [MeasurementId],
                 [Date],
                 [Time],
                 [MeasuredValue],
                 [AddedInsulinId]
        FROM     [dbo].[Measurement]
        ORDER BY [MeasurementId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Measurement] OFF;
    END

DROP TABLE [dbo].[Measurement];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Measurement]', N'Measurement';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Foreign Key unnamed constraint on [dbo].[AddedInsulin]...';


GO
ALTER TABLE [dbo].[AddedInsulin] WITH NOCHECK
    ADD FOREIGN KEY ([InsulinId]) REFERENCES [dbo].[Insulin] ([InsulinId]);


GO
PRINT N'Creating Foreign Key unnamed constraint on [dbo].[Measurement]...';


GO
ALTER TABLE [dbo].[Measurement] WITH NOCHECK
    ADD FOREIGN KEY ([AddedInsulinId]) REFERENCES [dbo].[AddedInsulin] ([AddedInsulinId]);


GO
PRINT N'Creating Foreign Key unnamed constraint on [dbo].[MeasurementFood]...';


GO
ALTER TABLE [dbo].[MeasurementFood] WITH NOCHECK
    ADD FOREIGN KEY ([FoodId]) REFERENCES [dbo].[Food] ([FoodId]);


GO
PRINT N'Creating Foreign Key unnamed constraint on [dbo].[MeasurementFood]...';


GO
ALTER TABLE [dbo].[MeasurementFood] WITH NOCHECK
    ADD FOREIGN KEY ([MeasurementId]) REFERENCES [dbo].[Measurement] ([MeasurementId]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
CREATE TABLE [#__checkStatus] (
    id           INT            IDENTITY (1, 1) PRIMARY KEY CLUSTERED,
    [Schema]     NVARCHAR (256),
    [Table]      NVARCHAR (256),
    [Constraint] NVARCHAR (256)
);

SET NOCOUNT ON;

DECLARE tableconstraintnames CURSOR LOCAL FORWARD_ONLY
    FOR SELECT SCHEMA_NAME([schema_id]),
               OBJECT_NAME([parent_object_id]),
               [name],
               0
        FROM   [sys].[objects]
        WHERE  [parent_object_id] IN (OBJECT_ID(N'dbo.AddedInsulin'), OBJECT_ID(N'dbo.Measurement'), OBJECT_ID(N'dbo.MeasurementFood'))
               AND [type] IN (N'F', N'C')
                   AND [object_id] IN (SELECT [object_id]
                                       FROM   [sys].[check_constraints]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0
                                       UNION
                                       SELECT [object_id]
                                       FROM   [sys].[foreign_keys]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0);

DECLARE @schemaname AS NVARCHAR (256);

DECLARE @tablename AS NVARCHAR (256);

DECLARE @checkname AS NVARCHAR (256);

DECLARE @is_not_trusted AS INT;

DECLARE @statement AS NVARCHAR (1024);

BEGIN TRY
    OPEN tableconstraintnames;
    FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
    WHILE @@fetch_status = 0
        BEGIN
            PRINT N'Checking constraint: ' + @checkname + N' [' + @schemaname + N'].[' + @tablename + N']';
            SET @statement = N'ALTER TABLE [' + @schemaname + N'].[' + @tablename + N'] WITH ' + CASE @is_not_trusted WHEN 0 THEN N'CHECK' ELSE N'NOCHECK' END + N' CHECK CONSTRAINT [' + @checkname + N']';
            BEGIN TRY
                EXECUTE [sp_executesql] @statement;
            END TRY
            BEGIN CATCH
                INSERT  [#__checkStatus] ([Schema], [Table], [Constraint])
                VALUES                  (@schemaname, @tablename, @checkname);
            END CATCH
            FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
        END
END TRY
BEGIN CATCH
    PRINT ERROR_MESSAGE();
END CATCH

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') >= 0
    CLOSE tableconstraintnames;

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') = -1
    DEALLOCATE tableconstraintnames;

SELECT N'Constraint verification failed:' + [Schema] + N'.' + [Table] + N',' + [Constraint]
FROM   [#__checkStatus];

IF @@ROWCOUNT > 0
    BEGIN
        DROP TABLE [#__checkStatus];
        RAISERROR (N'An error occurred while verifying constraints', 16, 127);
    END

SET NOCOUNT OFF;

DROP TABLE [#__checkStatus];


GO
PRINT N'Update complete.';


GO
